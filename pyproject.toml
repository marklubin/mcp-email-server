[project]
name = "mcp-email-server"
version = "1.0.0"
description = "Minimal MCP Email Server for ProtonMail Bridge"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "aioimaplib>=2.0.0",
    "aiosmtplib>=3.0.0",
    "uvicorn>=0.30.0",
    "python-dotenv>=1.0.0",
]
